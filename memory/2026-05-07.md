# Memory — 2026-05-07

## Critical Fix: Queue Processing (ROOT CAUSE of empty DB)
- `AUTO_PROCESS_QUEUE` was `false` in .env → queue files never processed → orders never reached Supabase
- Fixed: set `true` + added 5min scheduler sweep
- Processed backlog: 32 orders now in DB
- Dashboard + daily report now have real data

## Critical Fix: Double QRIS (Doku + Pakasir)
- Queue processor had `shouldAutoCreateQris()` that created Pakasir QRIS for every draft order
- With `AUTO_PROCESS_QUEUE=true`, this caused double QR (Doku from sync-state + Pakasir from queue)
- Fixed: `shouldAutoCreateQris()` returns false when `QRIS_PROVIDER=doku`

## QRIS E2E Flow — VERIFIED WORKING ✅
- Bot: write state → exec sync → **NO_REPLY** (confirmed in transcript)
- Backend: generate Doku QRIS → send QR to WA
- Customer pays → Doku webhook (paid=false initial) → Poller confirms
- Poller: confirm → push Pawoon → send receipt to WA
- Receipt: items ✅, total ✅, meja ✅, orderId ✅
- Bot does NOT send "terverifikasi" or fake receipt anymore

## Timezone Reminder
- Server: UTC+8 (Asia/Shanghai)
- WIB (Acid/customer): UTC+7
- Difference: server is 1 hour AHEAD of WIB
- When Acid says "12:43 WIB" = 13:43 server time
- All customer-facing times must be WIB

## Order ID Format
- Fixed: use `en-GB` locale → `1436` not `14.36`
- AGENTS.md updated with explicit instruction

## Auto-Suggest Upsell (added)
- Kopi tanpa makanan → suggest snack
- Makanan tanpa minuman → suggest kopsu
- Max 1 suggest per order, 1 line, nggak maksa
- Skip if customer says "cukup" or order < Rp10K

## Ongkir Report (added)
- `node backend/ongkir-report.js [date]` — standalone
- Also integrated into daily-report.js
- Scans state files (active + expired) for deliveryFee

## Git Checkpoint
- Pushed: `392f6ce..3fa544a main -> main` (55 files)
- Commit: "fix: major production fixes + new features (May 5-7)"

## Ardinals Update
- KYA delegated staking still NOT propagating to Ardi worknet after 48+ hours
- Disabled hourly check cron (waste of resource)
- Options: buy+stake ($81) or ask in Discord
- AWP price was $0.0081 on May 5

## Doku Payment Flow Notes
- Doku webhook sends `paid: false` as initial notification (not settled yet)
- Poller is the reliable confirmation source (queries Doku API directly)
- Webhook `paid: true` is rare/delayed — don't rely on it for Pawoon push
- Poller confirms → pushes Pawoon + sends receipt
